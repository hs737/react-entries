Steps to get the web page started

mongod
gulp
web pack
environment variables
node

onKeyPress={(e) => this.handleKeyPress(SUBMIT_ENUM.SIGNIN, e)}
onKeyPress={(e) => this.handleKeyPress(SUBMIT_ENUM.REGISTER, e)}
